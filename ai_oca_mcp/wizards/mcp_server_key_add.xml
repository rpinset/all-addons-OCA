<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 Dixmit
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>

    <record model="ir.ui.view" id="mcp_server_key_add_form_view">
        <field name="model">mcp.server.key.add</field>
        <field name="inherit_id" ref="mcp_server_key_form_view" />
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <form position="attributes">
                <attribute name="create">false</attribute>
                <attribute name="edit">false</attribute>
                <attribute name="delete">false</attribute>
            </form>
            <header position="attributes">
                <attribute name="invisible">1</attribute>
            </header>
            <field name="server_id" position="after">
            <field
                    name="key"
                    widget="CopyClipboardChar"
                    attrs="{'invisible': [('key', '=', False)]}"
                />
            </field>
            <form position="inside">
                <footer>
                    <button
                        name="generate_key"
                        type="object"
                        string="Generate Key"
                        class="btn-primary"
                        attrs="{'invisible': [('key', '!=', False)]}"
                    />
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record model="ir.actions.act_window" id="mcp_server_key_add_act_window">
        <field name="name">Add Security Key</field>
        <field name="res_model">mcp.server.key.add</field>
        <field name="view_mode">form</field>
        <field name="context">{}</field>
        <field name="target">new</field>
    </record>


</odoo>

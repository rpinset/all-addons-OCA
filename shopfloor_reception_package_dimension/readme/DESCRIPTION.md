A glue module between `shopfloor_reception` and `stock_quant_package_dimension`.

It adds on the `set_destination` screen of the scenario.

  * The height of the package and allows to change the value if needed,
    the new value is saved on the destination scan.

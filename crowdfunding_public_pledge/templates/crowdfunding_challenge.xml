<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2025 Hunki Enterprises BV
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl-3.0) -->
<data>
    <template
        id="template_challenge_detail"
        inherit_id="crowdfunding.template_challenge_detail"
    >
        <section position="after">
            <t
                t-set="public_pledge_invoices"
                t-value="object.sudo().invoice_ids.filtered(lambda x: x.payment_state == 'paid' and x.crowdfunding_public).sorted(lambda x: -x.amount_total)"
            />
            <section
                name="crowdfunding_public_pledge"
                t-if="public_pledge_invoices"
                class="mb24 crowdfunding-public-pledge"
            >
                <h2>Pledges</h2>
                <div class="container d-flex flex-wrap">
                    <div
                        t-foreach="public_pledge_invoices"
                        t-as="invoice"
                        class="card col-12 col-md-6 pledge"
                    >
                        <div class="card-body">
                            <a
                                class="row mx-0"
                                t-attf-href="{{object.website_url}}/pledge/{{invoice.partner_id.commercial_partner_id.id}}"
                            >
                                <t
                                    t-set="partner"
                                    t-value="invoice.partner_id.commercial_partner_id"
                                />
                                <img
                                    t-if="partner.image_128"
                                    t-att-src="image_data_uri(partner.image_128)"
                                    class="col mr-2"
                                    width="128"
                                />
                                <img
                                    t-if="not partner.image_128"
                                    src="/base/static/img/company_image.png"
                                    class="col mr-2"
                                    width="128"
                                />
                                <div class="col">
                                    <div t-field="partner.name" />
                                    <div t-field="invoice.amount_total" />
                                </div>
                            </a>
                        </div>
                    </div>
                </div>
            </section>
        </section>
    </template>
    <template id="pledge_detail">
        <t t-call="website.layout">
            <t t-set="additional_title" t-value="object.name" />
            <div id="wrap">
                <t t-call="website.record_cover">
                    <t t-set="_record" t-value="object" />
                    <t t-set="snippet_autofocus" t-value="True" />
                    <t t-set="use_filters" t-value="True" />
                    <t t-set="use_size" t-value="True" />
                    <t t-set="display_opt_name">Challenge Cover</t>
                    <div class="container">
                        <h1 t-field="object.name" class="py-4" />
                    </div>
                </t>
                <div class="container">
                    <section class="m-4 crowdfunding-public-pledge">
                        <div class="row mx-0 pledge">
                            <div class="col mr-2 flex-grow-0">
                                <img
                                    t-if="partner.image_128"
                                    t-att-src="image_data_uri(partner.image_128)"
                                    width="128"
                                />
                                <img
                                    t-if="not partner.image_128"
                                    src="/base/static/img/company_image.png"
                                    width="128"
                                />
                            </div>
                            <div class="col m-4">
                                    <b t-field="partner.name" /> has contributed <b
                                t-out="pledge_amount"
                                t-options="{'widget': 'monetary', 'display_currency': currency}"
                            /> to <a
                                t-att-href="object.website_url"
                                t-out="object.name"
                            />
                                    <h1>
                                <a
                                    t-attf-href="{{object.website_url}}/pay"
                                >Add your pledge today!</a>
                            </h1>
                                </div>
                        </div>
                    </section>
                </div>
            </div>
        </t>
    </template>
</data>

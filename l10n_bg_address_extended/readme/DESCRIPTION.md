This module extends the base address model to better match Bulgarian address
formatting needs. It adds separate fields for building, floor, and sector,
keeping them aligned with the main street line.

It also keeps company and partner address data in sync, so updates to structured
address fields are reflected consistently across both records.

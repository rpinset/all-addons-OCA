<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2026 Binhex <https://www.binhex.cloud>
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>

    <record id="wizard_social_account_view_form" model="ir.ui.view">
        <field name="name">Associate Social Account</field>
        <field name="model">wizard.social.account</field>
        <field name="arch" type="xml">
            <form>
                <field
                    name="image"
                    widget="image"
                    class="oe_avatar"
                    invisible="context.get('social_update_account', False)"
                />
                <div class="oe_title mb24">
                    <h1>
                        <field
                            name="media_id"
                            invisible="context.get('social_update_account', False)"
                            readonly="1"
                            options="{'no_open': True}"
                        />

                        <field
                            name="account_id"
                            readonly="1"
                            options="{'no_open': True}"
                            invisible="not context.get('social_update_account', False)"
                        />
                    </h1>
                </div>
                <group name="social_account_action">
                    <field
                        name="update_keys"
                        widget="boolean_toggle"
                        invisible="not context.get('social_update_account', False)"
                    />
                    <field
                        name="update_token"
                        widget="boolean_toggle"
                        invisible="not context.get('social_update_account', False)"
                    />
                </group>
                <group name="social_account_data">
                    <!-- Declared once here: every connector aims its own
                         fields at this group and reads media_type in their
                         invisible and required expressions. -->
                    <field name="media_type" invisible="1" />
                </group>
                <footer>
                    <button
                        name="action_associate_social_account"
                        string="Associate"
                        type="object"
                        class="btn-primary"
                        data-hotkey="q"
                        title="Link the selected account with Odoo. From then on it can be published on, and its figures are brought in."
                        invisible="context.get('social_update_account', False)"
                    />
                    <button
                        name="action_update_account"
                        string="Update"
                        type="object"
                        class="btn-primary"
                        data-hotkey="q"
                        title="Authorize this account again and refresh the keys and the token it publishes with. The account and its history are kept."
                        invisible="not context.get('social_update_account', False)"
                    />
                    <button
                        string="Cancel"
                        class="btn-secondary"
                        special="cancel"
                        data-hotkey="x"
                    />
                </footer>
            </form>
        </field>
    </record>
</odoo>

- On invoices, only some supported document types are linked to the document. The
  default supported types are PDF, CSV, Excel/OpenDocument spreadsheets,
  Word/OpenDocument documents, PowerPoint/OpenDocument presentations, and
  parseable XML files. Chatter-only attachments are therefore not selectable
  by this module.

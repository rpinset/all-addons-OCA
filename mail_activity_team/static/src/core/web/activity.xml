<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t
        t-name="mail_activity_team.Activity"
        t-inherit="mail.Activity"
        t-inherit-mode="extension"
    >
        <!-- Show team in the condensed view -->
        <xpath
            expr="//div[hasclass('o-mail-Activity-info')]/button[hasclass('btn-primary')]"
            position="before"
        >
            <span
                class="o-mail-Activity-user px-1"
                t-if="props.activity.team_id?.name"
            >(Team <t t-esc="props.activity.team_id.name" />)
            </span>
        </xpath>
        <!-- Show team in the expanded view -->
        <xpath expr="//t[@t-esc='props.activity.user_id.name']/../.." position="after">
            <tr t-if="props.activity.team_id?.name">
                <td class="text-end fw-bolder">Team</td>
                <td>
                    <t t-esc="props.activity.team_id.name" />
                </td>
            </tr>
        </xpath>
    </t>

</templates>

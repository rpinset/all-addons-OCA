=========================================
Field Service Route Availability (Legacy)
=========================================

This module was merged into ``fieldservice_availability`` in version 19.0.
It remains available as an installable compatibility stub so databases that
used the former module can upgrade without losing the module name. The merged
functionality is provided by the ``fieldservice_availability`` dependency.

New installations should install ``fieldservice_availability`` directly.


